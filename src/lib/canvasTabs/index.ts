/**
 * Canvas Tabs System
 *
 * Complete canvas tab navigation system for workspace management.
 *
 * Canvas = Workspace for a single item (template, document, design)
 * Canvas Tab = Visual representation in tab bar
 * Canvas Tabs Control = UI component showing all open canvas tabs
 */

// Export everything from core
export * from './core';

// Export everything from integrations
export * from './integrations';
